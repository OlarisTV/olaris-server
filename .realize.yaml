settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: olaris-server
  path: .
  commands:
    generate:
      status: true
    build:
      status: true
      dir: cmd/olaris-server
    run:
      status: true
      path: cmd/olaris-server
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
